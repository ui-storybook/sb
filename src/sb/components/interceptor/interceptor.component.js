import template from './interceptor.html';
import controller from './interceptor.controller';
import './interceptor.less';

let interceptorComponent = {
  bindings: {},
  controllerAs: 'vm',
  template,
  controller
};

export default interceptorComponent;
