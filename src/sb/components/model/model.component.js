import template from './model.html';
import controller from './model.controller';
import './model.less';

let modelComponent = {
  bindings: {},
  controllerAs: 'vm',
  template,
  controller
};

export default modelComponent;
