import template from './storyList.html';
import controller from './storyList.controller';
import './storyList.less';

let storyListComponent = {
  bindings: {},
  controllerAs: 'vm',
  template,
  controller
};

export default storyListComponent;
